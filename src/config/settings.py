# Dashboard & plotting settings
UI_INTERVAL_MS = 100        # UI refresh rate (ms)
MAX_DISPLAY_PTS = 200       # max points plotted per graph
FFT_WINDOW_SECONDS = 5.0    # FFT window length
FFT_UPDATE_EVERY_N_INTERVALS = 8  # recompute FFT every N intervals
SKIP_FIRST_SECONDS = 0.0    # show immediately
DISPLAY_SECONDS = 3.0       # display window for time plot

# App network
HOST = '0.0.0.0'
PORT = 7777
DEBUG = True
DEV_TOOLS_UI = False
